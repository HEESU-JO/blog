---
title: "Eigenvalue & Eigenvector"
date: 2025-01-05
categories: [linear-algebra]
tags: [eigenvalue, eigenvector, linear-algebra, machine-learning]
---

<script>
window.MathJax = {
  tex: {
    inlineMath: [['$', '$'], ['\\(', '\\)']],
    displayMath: [['$$','$$'], ['\\[','\\]']]
  }
};
</script>
<script id="MathJax-script" async
  src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js">
</script>

## 1️⃣ 왜 Eigenvalue와 Eigenvector를 배우는가?

선형대수에서 행렬은 단순한 숫자 배열이 아니라  
**벡터 공간을 변환하는 연산자(linear transformation)**이다.

어떤 벡터 공간이 linear transformation 되면 rotation,reflection, stretching 등 선형적인 공간 변환이 일어난다. 

> 이런 변환에서 방향은 유지하면서 단지 길이만 변환(stretching)되는 벡터가 존재한다. 

바로 **고유벡터(Eigenvector)**이다.
또한, 그 방향으로 얼마나 늘어나거나 줄어드는지를 나타내는 값이 **고유값(Eigenvalue)**이다.

이 개념은 단순한 이론을 넘어  
- PCA (주성분 분석)
- 선형 미분방정식
- Markov Chain
- 머신러닝 모델 안정성 분석  

등에서 핵심적인 역할을 한다.

---

## 2️⃣ 직관적 의미: 방향은 그대로, 크기만 변한다

행렬 $A$는 벡터를 다른 벡터로 변환한다.  
대부분의 경우 벡터는
- 방향이 바뀌고
- 길이가 바뀐다.

하지만 어떤 특별한 벡터들은  
행렬을 곱해도 **방향은 그대로 유지**되고  
**크기만 스칼라 배**로 변한다.

이 벡터가 바로 **고유벡터**다.

---

## 3️⃣ 수학적 정의

고유값과 고유벡터는 다음 식으로 정의된다.

$$
A\mathbf{v} = \lambda \mathbf{v}
$$

- $A$ : 정사각행렬
- $\mathbf{v} \neq \mathbf{0}$ : 고유벡터
- $\lambda$ : 고유값

이 식의 의미는 다음과 같다.

> 행렬 $A$를 벡터 $\mathbf{v}$에 적용했더니  
> 새로운 방향으로 이동하지 않고  
> **원래 벡터에 어떤 스칼라 $\lambda$만 곱해졌다**

즉,
- 방향은 보존되고
- 크기(부호 포함)만 변한다.

---

## 4️⃣ 왜 정사각행렬에서만 정의될까?

고유벡터는  
**변환 전 벡터와 변환 후 벡터를 비교**하는 개념이다.

이를 위해서는  
- 입력 벡터 $\\mathbf{v}$
- 출력 벡터 $A\\mathbf{v}$

가 반드시 **같은 벡터 공간**에 있어야 한다.

정사각행렬 $A \\in \\mathbb{R}^{n \\times n}$일 때만  
입력과 출력이 모두 $\\mathbb{R}^n$에 존재한다.

그래서 고유값과 고유벡터는  
**정사각행렬에서만 정의된다.**

---

## 5️⃣ 고유값은 어떻게 구할까?

정의식에서 출발해 보자.

$$
A\mathbf{v} = \lambda \mathbf{v}
$$

이를 정리하면

$$
(A - \lambda I)\mathbf{v} = \mathbf{0}
$$

이 방정식이 의미를 가지려면  
영벡터가 아닌 해가 존재해야 한다.

선형대수에서 이 조건은 다음과 같다.

$$
\det(A - \lambda I) = 0
$$

이 식을 **특성방정식(characteristic equation)**이라 하며,  
이를 풀어서 얻는 $\lambda$들이 바로 **고유값**이다.

---

## 6️⃣ 고유벡터는 어떻게 구할까?

고유값 $\lambda$를 하나 정하면  
다음 연립방정식을 푼다.

$$
(A - \lambda I)\mathbf{v} = \mathbf{0}
$$

이는 결국 $A - \lambda I$의 **영공간(null space)**을 구하는 문제다.

따라서 고유벡터는  
- 하나만 존재하는 것이 아니라 스칼라 배까지 포함해 무한히 많다.
- 즉, 하나의 고유벡터가 주어지면  
  그 스칼라 배 전체(span)가 모두 고유벡터가 된다. 

보통은 그 방향을 대표하는 벡터 하나만 선택한다.

---

## 7️⃣ 고유공간 (Eigenspace)

고유값 $\lambda$에 대응하는 모든 고유벡터(영벡터 포함)의 집합을  
**고유공간(eigenspace)**이라 한다.

$$
E_\lambda = \{ \mathbf{v} \mid A\mathbf{v} = \lambda \mathbf{v} \}
$$

이 집합은 항상 **벡터공간**이며,  
차원은 해당 고유값의 **기하적 중복도**를 의미한다.

---

## 8️⃣ 대각화(Diagonalization)와 고유값·고유벡터의 관계

행렬 $A \in \mathbb{R}^{n \times n}$가 다음과 같이 표현될 수 있을 때

$$
A = PDP^{-1}
$$

우리는 행렬 $A$가 **대각화 가능(diagonalizable)**하다고 말한다.

여기서 각 행렬의 의미는 다음과 같다.

- $P$ : 행렬 $A$의 **고유벡터들을 열벡터로 모아 만든 행렬**
- $D$ : 대응되는 **고유값들이 대각에 놓인 대각행렬**
- $P^{-1}$ : 고유벡터 기저에서 표준기저로 되돌리는 변환

---

### 🔹 이 식이 의미하는 바 (기하학적 해석)

이 분해는 단순한 행렬 조작이 아니라  
**좌표계의 변화**를 의미한다.

1. $P^{-1}$  
   → 표준기저에서의 벡터를  
   **고유벡터 기저 좌표**로 변환

2. $D$  
   → 각 고유벡터 방향으로  
   **고유값만큼 스케일링**

3. $P$  
   → 다시 원래 좌표계로 복원

즉,

> 행렬 $A$의 복잡한 선형 변환을  
> “방향별 스케일 조정”으로 완전히 분해한 것이다.

---

### 🔹 왜 대각행렬이 좋은가?

대각행렬 $D$는 계산이 극도로 단순하다.

- 행렬 거듭제곱
  $$
  D^k =
  \begin{pmatrix}
  \lambda_1^k & & \\
  & \ddots & \\
  & & \lambda_n^k
  \end{pmatrix}
  $$

- 행렬 함수 (exponential 등)
  $$
  e^D =
  \begin{pmatrix}
  e^{\lambda_1} & & \\
  & \ddots & \\
  & & e^{\lambda_n}
  \end{pmatrix}
  $$

그래서

$$
A^k = P D^k P^{-1}
$$

같은 계산이 가능해진다.

---

### 🔹 대각화 가능 조건: 왜 고유벡터가 n개 필요할까?

행렬 $A$가 $n \times n$이라면  
공간의 차원은 $n$이다.

이 공간 전체를 표현하려면  
**서로 선형독립인 벡터가 정확히 $n$개** 필요하다.

대각화에서:

- $P$는 **기저 변환 행렬**
- $P^{-1}$이 존재하려면
  → $P$의 열벡터들이 선형독립이어야 함

즉,

> **고유벡터가 $n$개 모여서  
> 기저(basis)를 이룰 수 있어야  
> 대각화가 가능하다.**

이 조건이 깨지면  
$P^{-1}$이 존재하지 않으며  
대각화는 불가능하다.

---

### 🔹 중복 고유값과 대각화 실패

고유값이 중복된다고 해서  
항상 대각화가 불가능한 것은 아니다.

중요한 것은  
**고유값의 개수**가 아니라  
**고유벡터의 개수**다.

- 대수적 중복도 ≥ 기하적 중복도
- 모든 고유값에 대해  
  기하적 중복도의 합 = $n$  
  ⟺ 대각화 가능

만약 어떤 고유값에 대해  
기하적 중복도가 부족하다면  
행렬은 대각화되지 않는다.

---

### 🔹 왜 이게 실전에서 중요한가?

1. **행렬 거듭제곱**
   - Markov Chain
   - 동적 시스템의 장기 거동 분석

2. **선형 미분방정식**
   - $\\dot{x} = Ax$의 해 표현
   - $e^{At}$ 계산 단순화

3. **PCA**
   - 공분산 행렬을 고유벡터 기저로 분해
   - 데이터 분산을 축 단위로 해석

즉, 대각화는  
**복잡한 선형 연산을 좌표계 문제로 바꾸는 핵심 도구**다.

---

### 🔹 한 문장 요약

> 행렬의 대각화란  
> 선형 변환을  
> “고유한 방향들에 대한 독립적인 스케일 변화”로  
> 완전히 분해하는 것이다.

---

## 마무리

고유값과 고유벡터는  
행렬이라는 복잡한 변환을  
**핵심적인 방향과 크기 변화**로 요약해 주는 도구다.

이 개념을 이해하면  
선형대수, 머신러닝, 데이터 분석이  
하나의 흐름으로 연결되기 시작한다.
